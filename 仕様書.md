# AIホームパートナー アバター会話デモ 仕様書

作成日: 2026-07-24 ／ 対象: チーム確認用（現状の実装内容 ＋ 今後の計画）

このドキュメントは「いま何ができているか（現状仕様）」と「これから何を考えているか（今後の計画）」を
まとめたものです。指示書（[claude-code_avatar-demo_指示書_v1.1.md](claude-code_avatar-demo_指示書_v1.1.md)）を
もとに、簡易デモとして実装した内容を反映しています。

---

# パート1: 現状仕様（いま動くもの）

## 1.1 概要・ゴール

高齢者見守りPWA「AIホームパートナー」の中核体験である「常時待機アバターとの音声会話」を、
コンテストのライブデモで使える形にした先行実装。

- アバターに話しかけると、音声で応答が返る（会話が往復する）。
- 「監視ではなくパートナー」という雰囲気が、実際に会話して伝わる。
- **完全ローカル完結**：AWS等の外部サービスを一切呼ばず、通信・課金・障害リスクがない。

## 1.2 動作環境

| 項目 | 内容 |
|---|---|
| 推奨ブラウザ | **Google Chrome**（音声認識が最も安定。Safariは不安定） |
| 実行URL | `http://localhost:5173`（開発時）／GitHub Pages（公開時・HTTPS） |
| 必要ソフト | Node.js v20以上、Chrome |
| マイク | 使用許可が必要（拒否時は画面上部に案内） |

## 1.3 画面構成

- **中央**: ネオン線画の猫の顔アバター（水色に発光、SVG）。
  - 3状態で表情が変化：
    - `待機(idle)`: にっこり閉じ目、ゆっくり呼吸するアニメ
    - `傾聴(listening)`: まん丸の大きな目、脈打つアニメ（聞いている）
    - `発話(speaking)`: 片目ウインク＋きらり、口が開閉するアニメ（話している）
- **字幕**: アバターの発話と、聞き取った本人の言葉を大きな文字で表示。
- **「話す」ボタン**: 大きなボタン。押すと聞き取り開始（自動起動が不安定な環境の保険）。
- **気分スコア（左上）**: 😊/😐/😟 ＋ 0〜100の数値。
- **設定ボタン（右上・ハンバーガー）**: 押すと設定パネルが右からスライドイン。
- **各種フィードバック表示**: 「本日 通知オフ」バッジ、「ご家族へ通知しました（デモ）」トースト、緊急バナー（赤・閉じるボタン付き）。
- **配色**: 明るい白〜淡色背景＋うっすらドット柄。高コントラスト・特大フォント・広い余白。

## 1.4 会話ロジック

聞き取ったテキストを、次の優先順位で判定して応答する（[matching.ts](src/matching.ts)）。

1. **controlPhrases（制御フレーズ）を最優先**で判定。一致したら専用応答＋画面フィードバック。
2. 次に **intents（通常の会話意図）** を判定。一致した応答候補からランダムに返し、気分スコアを更新。
3. どれにも当たらなければ **fallback** からランダムに返答。
4. 応答後は**自動でまた聞き取りに戻る**（ターンテイキング。会話が続く）。

キーワードは単純な部分一致。判定は純粋関数として切り出してあり、将来LLM等に差し替え可能。

### 制御フレーズと画面フィードバック（[conversation.json](src/data/conversation.json)）

| フレーズ例 | 動作(action) | 画面フィードバック（デモ・実送信なし） |
|---|---|---|
| 「今日は病院」「通院」 | suppressNotifications | 「🏥 本日 通知オフ（デモ）」バッジ |
| 「気分が悪い」「しんどい」「痛い」 | notifyFamily | 「📩 ご家族へ通知しました（デモ）」トースト |
| 「助けて」「たすけて」 | emergency | 赤い緊急バナー（**閉じるボタンで手動クローズ**） |

## 1.5 気分スコア（モック分析）

- 初期値50（普通）。intent一致時、positiveで+15／negativeで-15（0〜100でクランプ）。
- スコアで絵文字を切替：60以上=😊／40以下=😟／中間=😐。
- 将来のAI分析（Rekognition＋Comprehend）が返す値の**プレースホルダ**。実装は差し替え可能な形。

## 1.6 音声の自然化（棒読み対策）

- **方針A（優先・器のみ用意）**: セリフに対応する事前生成MP3があれば再生する仕組み。現状は音源未登録のため未使用。
- **方針B（現在有効）**: ブラウザ内蔵音声を自然寄りに調整。
  - ja-JPの声から Natural/Online/Google を優先選択。
  - 速度 `rate=0.95`（やや遅め）、高さ `pitch=1.0`。
  - 句読点（。！？、）で区切り、間（ま）を空けて読む。
- 認識と合成が重ならないよう制御。発話中は聞き取りを止める。

## 1.7 設定メニュー

- 右上ハンバーガー → 設定パネル（右からスライドイン、背景は暗幕）。
- **項目は「通知」のみ**。押せるが**現状は何も起きない**（後日実装予定のプレースホルダ）。
- ×ボタンまたは背景クリックで閉じる。

## 1.8 技術構成・ディレクトリ

- React + Vite + TypeScript、状態管理は `useState` のみ（外部ライブラリなし）。
- 音声はブラウザ標準 Web Speech API。PWA対応（`vite-plugin-pwa`）。
- 見た目（アバター等）はロジックと分離し、デザイン差し替えを想定。

```
src/
  components/      画面パーツ（Avatar / Captions / Controls / StatusBadges / SettingsMenu）
  hooks/           useSpeechRecognition / useSpeechSynthesis / useConversation
  services/        analysisService(モック) / notificationService(モック) / index(mock切替)
  data/            conversation.json（会話ツリー。非エンジニアが編集可）
  assets/audio/    事前生成音声の置き場と対応表 manifest.ts（現状空）
  matching.ts      応答判定（純粋関数）
  types.ts / App.tsx
```

会話内容は `conversation.json`（openings / intents / controlPhrases / fallback）を編集するだけで、
コードを触らず変更できる。

## 1.9 非機能・制約（重要）

- **外部通信ゼロ**：`fetch`/`axios`等の外部API呼び出しなし。ビルド成果物を検査済み。
- **課金ゼロ**：AWS不使用、音声はブラウザ内蔵、GitHub Pages（公開リポジトリ）無料。
- **モック実装のみ**：AWS SDK・Cognito・Lambda・DynamoDB等は一切含まない。
- 環境変数 `VITE_BACKEND_MODE=mock` で将来のmock/aws切替に備える（現状は常にmock）。

## 1.10 現状の受け入れ基準（Doneの目安）

- [x] 起動するとアバターが日本語で話しかける。
- [x] 「話す」→聞き取り→日本語で応答（会話が複数ターン続く）。
- [x] 制御フレーズで専用応答＋画面フィードバックが出る。
- [x] 気分スコアが会話に応じて表示・変化する。
- [x] 会話内容が `conversation.json` の編集だけで変えられる。
- [x] 主要セリフが極端な棒読みでなく読み上げられる。
- [x] 外部通信が一切発生しない。
- [x] マイク不許可・非対応ブラウザで案内が出る。
- [x] ネオン猫アバター（3状態）＋明るいドット背景＋設定メニュー（通知のみ）。

---

# パート2: 今後の計画・検討事項

> 優先度や実施可否は未確定。実装前に都度確認します。

## 2.1 近いうちに（短期）

| 項目 | 内容 | 補足 |
|---|---|---|
| 通知設定画面 | 設定メニューの「通知」を実際に機能させる | 現状は押せるだけのプレースホルダ |
| 音声MP3の同梱（方針A） | 主要セリフをニューラルTTSで事前生成し、自然な声で再生 | 音源作成は開発時の別作業。再生は同梱ファイルのみ（外部通信なし） |
| アバターのデザイン差し替え | デザイン担当のFigmaカンプに合わせて猫SVGを差し替え | 見た目はロジックと分離済みなので差し替え容易 |

## 2.2 中期（余力があれば）

- 設定メニューの他項目（プロフィール／アバター設定／プライバシー／ヘルプ）。
- アバターのカスタマイズUI（目の色・大きさ・透明度・口の形・かざり）。※モックアップに構想あり。
- 応答バリエーションの拡充（`conversation.json` の充実、リサーチ担当が編集）。

## 2.3 将来（クラウド連携フェーズ）

**今回は一切実装しない。** ただし後から「差し込むだけ」で拡張できる設計にしてある。

- `services/` 層をAWS実装に差し替え：
  - `analysisService` → Lambda経由で Rekognition / Transcribe / Comprehend（表情・音声・感情分析）。
  - `notificationService` → Lambda経由で SNS（家族への実通知）。
- 認証（Cognito）、家族用ダッシュボード、カメラによる表情分析、起床/就寝のスポット分析。
- PWAとしてタブレット常設運用へ発展。

### 💰 クラウド連携時の課金・運用の注意

- AWSを足すと**従量課金が発生**。常時分析せず「話しかけたターン」「起床/就寝のスポット」に限定する。
- **AWS Budgets で予算アラートを必ず設定**。無料枠の範囲を把握する。
- ライブデモ会場では意図せぬ課金・障害を避けるため `VITE_BACKEND_MODE=mock` で動かす。
- フロントにAWS SDK・秘密情報を持ち込まない（Lambda Function URL を `fetch` で叩くだけにする）。

## 2.4 未確定・要確認事項

- GitHub リポジトリ名（現状 `ai-home-partner-app` 前提。`package.json` の `homepage` は要記入）。
- 事前生成音声（MP3）の素材と作成手段（Amazon Polly か、無料ツールか）。
- 正式なアバターのデザインカンプ（Figma）の提供時期。
- 通知設定画面の具体的な仕様（何をON/OFFできるか）。

---

## 参考ファイル

- ローカル起動・配信手順・チェックリスト … [README.md](README.md)
- 会話データ … [src/data/conversation.json](src/data/conversation.json)
- 元の指示書 … [claude-code_avatar-demo_指示書_v1.1.md](claude-code_avatar-demo_指示書_v1.1.md)
