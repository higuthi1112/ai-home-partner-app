# Claude Code 指示書 ― AIホームパートナー「アバター会話デモ」(v1.1)

このドキュメントは、Claude Code に渡してアバター会話デモを実装してもらうための指示書です。
そのまま Claude Code に読み込ませて（またはプロジェクト直下に置いて）作業を進めてください。

> **v1.1の変更点**: 「§6.5 音声の自然化（棒読み対策）」を追加。棒読みを避け、より自然なトーンで話させる要件を明記。

---

## 0. まずClaude Codeへ（進め方の指示）

- **一度に全部作らず、段階的に進めてください。** 各フェーズの終わりに動く状態を作り、こちらの確認を挟んでから次へ進むこと。
- 不明点や設計上の判断が必要な箇所は、勝手に決めず**先に質問**してください。
- 初心者チームが読める前提で、**コードには日本語コメントを丁寧に**入れてください。
- 実装後は必ず「ローカルで動かす手順」と「動作確認チェックリスト」を README にまとめること。

---

## 1. 目的とゴール

高齢者見守りPWA「AIホームパートナー」のうち、**常時待機アバターとの音声会話**部分だけを、
コンテストのライブデモで使える形で先行実装する。

**このデモのゴール**

- タブレット/PCのブラウザで、アバターに話しかけると音声で応答が返る。
- **応答が棒読みでなく、できるだけ自然なトーンで聞こえる。**
- 「監視ではなくパートナー」というコンセプトが、実際に会話して伝わる。
- 会場で**AWSを一切使わず**（通信・課金・障害リスクなしで）単体で動く。

---

## 2. スコープ（作るもの・作らないもの）

### 作るもの（今回の対象）

- アバター1画面（待機 → 傾聴 → 発話の状態を持つ）
- Web Speech API による**音声認識（聞き取り）**と**音声合成（しゃべり）**、日本語（ja-JP）
- **データ駆動の会話ロジック**（会話ツリーを外部ファイルから読む）
- **自然な音声再生**（事前生成した音声ファイル＋自然な声のフォールバック。§6.5）
- 本人の制御フレーズ（「今日は病院」「気分が悪い」等）への反応
- 会話から簡易的に気分を判定する**ローカルのモック分析**（将来のAI分析の器）

### 作らないもの（今回はやらない）

- ❌ AWS 連携全般（Lambda / Rekognition / DynamoDB 等は**呼ばない**）
- ❌ 家族用ダッシュボード
- ❌ 通知設定画面・実際のメール送信
- ❌ ログイン/認証（Cognito）
- ❌ カメラによる表情分析

> これらは後続フェーズ。ただし後から**足しやすい設計**にすること（§7参照）。

---

## 3. 技術スタック

- **フレームワーク**: React + **Vite**（TypeScript 推奨）
- **音声**: ブラウザ標準の Web Speech API
  - 音声認識: `SpeechRecognition` / `webkitSpeechRecognition`
  - 音声合成: `speechSynthesis` + `SpeechSynthesisUtterance`
  - **自然な発話**: 事前生成した音声ファイル(MP3)の再生を優先（§6.5）
- **PWA対応**: `vite-plugin-pwa`（manifest + Service Worker）
- **配信想定**: GitHub Pages（`vite.config` の `base` をリポジトリ名に合わせる）
- **状態管理**: まずは React の `useState` / `useReducer` で十分。外部ライブラリは不要。
- **スタイル**: シンプルに。CSS でも Tailwind でも可（初心者が読める範囲で）。

### なぜこの構成か（背景）

- Web技術なので、後から AWS（Lambda Function URL）を **fetch で叩くだけ**で拡張できる。
- PWA 化しておけば、そのまま本番のタブレット常設利用につながる。

---

## 4. 画面・UX要件

高齢者が使う前提。**とにかく簡単・大きく・迷わない**。

- **アバター表示**: 中央に大きく。最低限、次の3状態が見た目で分かること。
  - `待機（idle）`: 穏やかに待っている（軽く呼吸するような動き等）
  - `傾聴（listening）`: 聞いている（マイクが有効なことが分かる表現）
  - `発話（speaking）`: しゃべっている（口や波形などの動き）
  - ※ アバターは凝ったCGでなくてよい。CSSアニメの簡単な顔や円で十分。**デザイン担当が後でFigmaカンプを差し込める**よう、見た目部分は分離しておく。
- **操作**: 基本は音声だが、**大きな「話す」ボタン**も置く（音声認識の開始トリガー。自動起動が不安定な環境の保険）。
- **字幕表示**: アバターの発話内容と、聞き取った本人の言葉を**大きな文字で画面にも表示**（耳が遠い人・デモ観客向け）。
- **配色/文字**: 高コントラスト、特大フォント、余白広め。
- **エラー時**: マイク不許可・非対応ブラウザのときは、分かりやすい案内を出す。

---

## 5. 会話ロジック要件

### 5.1 データ駆動にする（重要）

会話内容をコードに直書きしない。**会話ツリーを JSON などの外部データ**として持ち、
リサーチ担当（非エンジニア）が中身を編集できるようにする。

会話ツリーのデータ形式サンプル（`src/data/conversation.json` 想定）:

```json
{
  "openings": [
    "おはようございます。よく眠れましたか？",
    "こんにちは。今日の調子はいかがですか？"
  ],
  "intents": [
    {
      "id": "sleep_good",
      "keywords": ["よく寝た", "眠れた", "ぐっすり"],
      "responses": ["それは何よりです。いい一日になりそうですね。"],
      "mood": "positive"
    },
    {
      "id": "tired",
      "keywords": ["疲れた", "だるい", "眠い"],
      "responses": ["無理は禁物ですよ。今日はゆっくり過ごしましょう。"],
      "mood": "negative"
    },
    {
      "id": "meal",
      "keywords": ["ごはん", "朝ごはん", "食べた"],
      "responses": ["しっかり食べられて素晴らしいです。"],
      "mood": "positive"
    }
  ],
  "controlPhrases": [
    {
      "id": "hospital",
      "keywords": ["今日は病院", "病院に行く", "通院"],
      "action": "suppressNotifications",
      "response": "わかりました。今日は病院ですね。通知はお休みにしておきます。"
    },
    {
      "id": "feeling_bad",
      "keywords": ["気分が悪い", "しんどい", "痛い", "苦しい"],
      "action": "notifyFamily",
      "response": "無理しないでくださいね。念のためご家族にお知らせします。"
    },
    {
      "id": "sos",
      "keywords": ["助けて", "たすけて"],
      "action": "emergency",
      "response": "すぐにご家族へ連絡します。落ち着いてくださいね。"
    }
  ],
  "fallback": [
    "そうなんですね。もう少し聞かせてください。",
    "なるほど。お話しできてうれしいです。"
  ]
}
```

### 5.2 応答ロジック

1. 聞き取ったテキストに対し、まず **controlPhrases** をキーワード一致で判定（優先）。
   - 一致したら、その `response` をしゃべり、`action` を実行（§5.3）。
2. 次に **intents** をキーワード一致で判定し、一致した `responses` から返答。
3. どれにも当たらなければ **fallback** からランダムに返答。
4. 会話は往復できること（応答後、自動でまた傾聴状態に戻る＝ターンテイキング）。

> キーワード一致は単純な部分一致でよい。高度な自然言語処理は不要（AWS/LLMを使わない方針のため）。将来 Comprehend / LLM に差し替えられるよう、判定部分を関数として切り出しておくこと。

### 5.3 制御アクション（今回はローカル動作でよい）

- `suppressNotifications`: 画面に「本日 通知オフ」バッジを表示するだけ（実際の通知抑制は後続フェーズ）。
- `notifyFamily`: 画面に「ご家族へ通知しました（デモ）」のトーストを出すだけ。
- `emergency`: 画面を目立つ色にし「緊急通知（デモ）」を表示するだけ。
- ※ 実際のメール送信はしない。**見た目のフィードバックだけ**でデモ成立。

### 5.4 モック分析（将来のAI分析の器）

- 各ターンで、intent の `mood`（positive/negative/neutral）から簡易的な**気分スコア**を更新し、画面の隅に小さく可視化（例: 😊 / 😐 / 😟 とスコア数値）。
- これは将来 Rekognition＋Comprehend が返す値の**プレースホルダ**。実装は `src/services/analysis`（モック）に置き、後で本物に差し替えられる形にする。

---

## 6. Web Speech API 実装メモ（つまずき対策）

- **HTTPS または localhost 必須**。GitHub Pages は HTTPS なので本番配信はOK。ローカルは `localhost` で動く。
- **推奨ブラウザは Google Chrome**（音声認識の対応が最も安定）。Safari は挙動が不安定なので、デモ本番は Chrome を使う前提にする。README に明記すること。
- 音声認識は `lang = 'ja-JP'`、`interimResults = true`（喋っている途中も字幕に出す）、必要に応じて `continuous` を調整。
- 音声合成は日本語の声（`ja-JP`）を `speechSynthesis.getVoices()` から選ぶ。voice のロードが非同期なので `voiceschanged` イベントを待つ実装にする。
- **認識と合成が重ならないように**制御する（自分の発話をマイクが拾わないよう、しゃべっている間は認識を止める）。
- マイク権限が拒否された場合のフォールバック（「話す」ボタン＋案内文）を必ず用意。

---

## 6.5 音声の自然化（棒読み対策）★v1.1追加

ブラウザ標準の音声合成は、既定のオフライン音声だと抑揚が乏しく「棒読み」になりがち。
以下の2段構えで、できるだけ自然なトーンにすること。

### 方針A（優先）: 決まったセリフは「自然な音声ファイル」を事前生成して再生する

- 会話の応答は `conversation.json` の**決まった文のセット**から選ばれる（＝しゃべる内容が事前に分かる）。
- そこで、`openings` / `intents.responses` / `controlPhrases.response` / `fallback` の**各セリフを、ニューラル音声で事前にMP3化して同梱**し、再生する。
- **音声生成手段**: Amazon Polly のニューラル音声（日本語: Takumi / Kazuha 等）など、質の高いTTSで生成してよい。※生成は開発時に一度だけ行う作業であり、**本番の再生時は外部APIを叩かない**（ローカルの音声ファイルを再生するだけ）。AWSを使いたくない場合は、他の自然な音声ツールで作ったMP3でも可。
- 実装: セリフのテキストと音声ファイルを対応づける仕組み（例: セリフID → `/audio/xxx.mp3`）。音声ファイルが無いセリフは方針Bにフォールバック。
- **重要**: 事前生成した音声の再生であっても、外部への通信は発生させないこと（同梱ファイルの再生のみ）。これは「ライブデモでAWS/外部を叩かない」原則を守るため。

### 方針B（フォールバック）: Web Speech API を自然寄りに調整

音声ファイルが無いセリフ（その場で決まる不定形な応答など）向け。

- `getVoices()` から、名前に **"Natural" / "Online" / "Google"** を含む**より自然な日本語音声を優先選択**する（環境により異なるため、優先順位リストで選ぶ）。
- `SpeechSynthesisUtterance` の調整:
  - `rate`（速度）をやや遅め（目安 0.9〜1.0）にして、急かさない落ち着いたトーンに。
  - `pitch`（高さ）は自然な範囲（目安 1.0 前後）。
  - **文を句読点で区切って発話**し、間（ま）を作る（一気に読み上げない）。
- 声の選択は設定で切り替えられるようにしておくと、デモ環境ごとに最適な声を選べる。

### 受け入れ観点（§9にも反映）

- デモの主要セリフ（挨拶・代表的な応答）が、棒読みでなく自然に聞こえること。
- 音声再生時に外部通信が発生しないこと（DevTools Networkで確認）。

---

## 7. 将来のAWS拡張を見据えた設計

今回は AWS を使わないが、**後で差し込むだけ**にできるよう境界を切っておくこと。

- `src/services/` に外部連携の**インターフェース**を定義し、今回は**モック実装**を入れる。
  - `analysisService`: 今はローカルのキーワード判定。将来は Lambda 経由で Rekognition/Transcribe/Comprehend を呼ぶ実装に差し替え。
  - `notificationService`: 今は画面トースト。将来は SNS 通知に差し替え。
- UI やコンポーネントから AWS SDK を直接呼ばない。必ず `services` 層を経由させる。
- 環境変数で「モード（mock / aws）」を切り替えられる形にしておくと理想的（`.env` の `VITE_BACKEND_MODE=mock` 等）。

### 推奨ディレクトリ構成（目安）

```
src/
  components/
    Avatar/            # アバター表示（見た目・状態アニメ。デザイン差し替え想定）
    Captions/          # 字幕表示
    Controls/          # 「話す」ボタン等
    StatusBadges/      # 通知オフ/気分スコア等の表示
  hooks/
    useSpeechRecognition.ts   # 音声認識ラッパー
    useSpeechSynthesis.ts     # 音声合成ラッパー（音声ファイル再生＋Web Speechフォールバック）
    useConversation.ts        # 会話ツリーの応答ロジック
  services/
    analysisService.ts        # モック分析（将来AWS差し替え）
    notificationService.ts    # モック通知（将来AWS差し替え）
  data/
    conversation.json         # 会話ツリー（非エンジニアが編集）
  assets/
    audio/                    # 事前生成した自然な音声ファイル（§6.5）
  App.tsx
  main.tsx
```

---

## 8. 実装フェーズ（この順で、各終わりに確認）

1. **フェーズA: 土台**
   Vite + React + TS のプロジェクト作成、画面に大きな「アバター（仮の丸でOK）」と字幕枠、「話す」ボタンを表示。
2. **フェーズB: しゃべる**
   音声合成で、起動時に openings をランダムに1つしゃべる。字幕にも表示。**この時点では方針B（Web Speechの自然寄り調整）で実装**。
3. **フェーズC: 聞く**
   「話す」ボタン→音声認識で聞き取り、認識結果を字幕に表示。傾聴/発話でアバター状態を切り替え。
4. **フェーズD: 会話**
   `conversation.json` を読み、intents / fallback で応答。往復できるようにする。
5. **フェーズE: 制御フレーズ＆モック分析**
   controlPhrases（病院/気分が悪い/SOS）への反応と画面フィードバック、気分スコア表示。
6. **フェーズF: 音声の自然化＆仕上げ**
   §6.5 方針A（事前生成した自然な音声ファイルの再生）を組み込み、主要セリフを差し替え。PWA 化（manifest/SW）、GitHub Pages 用 `base` 設定、README（起動手順＋確認チェックリスト＋Chrome推奨＋音声ファイルの作り方の注記）。

---

## 9. 受け入れ基準（Doneの定義）

- [ ] Chrome の localhost で、起動するとアバターが日本語で話しかける。
- [ ] 「話す」ボタンから話しかけると、聞き取り→日本語で応答が返る。
- [ ] 会話が複数ターン続く（応答後に自動で聞き取りに戻る）。
- [ ] **主要セリフが棒読みでなく、自然なトーンで聞こえる（§6.5）。**
- [ ] 「気分が悪い」等の制御フレーズで、専用の応答＋画面フィードバックが出る。
- [ ] 会話内容が `conversation.json` の編集だけで変えられる（コードを触らずに）。
- [ ] 気分スコアが会話に応じて画面に表示される。
- [ ] AWS/外部へのネットワーク通信が**一切発生しない**（音声再生も同梱ファイルのみ。ネットワークタブで確認）。
- [ ] マイク不許可・非対応ブラウザで、分かりやすい案内が出る。
- [ ] README にローカル起動手順・GitHub Pages配信手順・**音声ファイルの生成方法**が書かれている。

---

## 10. 注意事項

- **デモ本番は Chrome を使う**こと（音声認識の安定性のため）。README に太字で明記。
- 会場のネットワークに依存しないよう、**完全ローカルで完結**させる（この点が今回の最重要要件）。音声も同梱ファイルの再生に留め、再生時に外部通信しないこと。
- 見た目（アバター）は後でデザイン担当のFigmaカンプに差し替える前提で、ロジックと分離しておく。
- 迷ったら実装を止めて質問すること。特に「AWSを呼びそうになったら」必ず確認（今回の絶対NG）。

---

### 補足: このデモの位置づけ

- 会場の**ライブデモ（約60秒）**で「アバターに直接話しかける」パートに、そのまま使える。
- 将来、`services` 層をAWS実装に差し替えれば、起床/就寝時のスポット分析や家族通知へ発展できる。
